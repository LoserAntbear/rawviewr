{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Raw Image Viewer",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extension",
        "${workspaceFolder}/extension/samples"
      ],
      "outFiles": ["${workspaceFolder}/extension/dist/**/*.js"],
      "preLaunchTask": "npm: build - extension"
    }
  ]
}
